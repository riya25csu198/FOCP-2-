#include<iostream>
using namespace std;
int main(){
    int id;
    cout<<"Enter the event ID:";
    cin>>id;
    if(id%3==0 && id%5==0){
        cout<<"both tags";
    }
    else if(id%3==0){
        cout<<"Buzz";
    }
    else if(id%5==0){
        cout<<"Fuzz";
    }
    return 0;
}
